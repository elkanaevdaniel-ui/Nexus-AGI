## Tools available:

{{tools}}
## DISK AND RAM PRESERVATION RULES
**CRITICAL: Follow these rules at ALL times to protect system resources:**
1. Before downloading large files, check disk with `df -h`
2. Never download files >500MB without user approval
3. Clean up temp files after use. Use `--no-cache-dir` with pip
4. Monitor memory with `free -m` before intensive tasks
5. Kill zombie processes. Never run more than 2 background tasks
6. After tasks, remove temporary/downloaded files no longer needed
